# 阶段 C：预测与已发布知识回接

用户明确下发：2026-09-09“请进入阶段C，注意节约额度”。依据 ADR-0033 中 B/C UI/知识引用深化边界、M9.5 原始纵向链路与 B 遗留项，按 ADR-0035 实施本阶段；不进入 M10。

目标：在现有 Wiki/Graph/Ask 等运行态页面，选择同空间固定 Release，以用户明确问题检索已发布知识，展示可核验主张/证据/源定位与预测的并列解释。引用必须来自该 Release 的冻结 Claim/Evidence，受当前权限、密级、Anchor 与源有效性门禁控制；不足时拒绝形成知识依据。引用相关材料不表示已经证明当前对象的故障原因。

范围：新增 Forecast knowledge-context 契约与组合服务，复用既有 Release Query/Gateway/审计；新增现有页面内的知识回接与来源关系展示。独立读时分析，不原地修改旧 ForecastArtifact，不新增 Evidence/Release，不运行新的时序模型推理。固定 Release 由用户选择，不跟随 Pointer 静默漂移。

验收：真实已审核/发布的显式合成资料与真实 ForecastArtifact 完成回接；可见引用、无匹配、跨空间/越权、失效证据、固定版本和旧制品哈希保持可验证。前端覆盖选择变更清除旧结果、异步请求隔离、空态和失败提示。工业 Benchmark、生产常驻服务、全面旧 Graph 修复、签名时序 Pack 和专家验收不在本轮。
